function clickModal(modalId) {
  // Asegúrate de que el modal se esté inicializando correctamente con Bootstrap
  $('#' + modalId).modal('show'); // Esto activará el modal con el ID pasado como argumento
}
